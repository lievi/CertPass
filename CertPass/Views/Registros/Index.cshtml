@model IEnumerable<Modelo.Perguntas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescPergunta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescPergunta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PerguntaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PerguntaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PerguntaId })
        </td>
    </tr>
}

</table>
